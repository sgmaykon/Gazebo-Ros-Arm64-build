# Gazebo-Ros-Arm64-build
A gazebo-ros-pkgs build for aarch64. Cloning this is faster than building from source

Created this repo due to lack of existing binaries of gazebo_ros for arm64 or packages. Once cloned, just 
`source install/setup.bash`
and you are good to go.
